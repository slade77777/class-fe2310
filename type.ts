export interface ProductInterface {
  createdAt: string,
  name: string,
  amount: number,
  price: number,
  expireTime: number,
  id: string
}

type UserType = string

export const a = 'test value'
